{
  "buildCommand": "pip install -r requirements.txt && python scripts/sync_illustrations.py && python scripts/validate_theme_tokens.py && python scripts/validate_content.py && pelican content -s publishconf.py && python scripts/generate_sitemap.py",
  "outputDirectory": "output",
  "installCommand": "pip install -r requirements.txt"
}
